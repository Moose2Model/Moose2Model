*RWMooseDiagram
moose2ModelRefresh
	<menuItem: 'Refresh (Clears all settings)' category: 'Moose2Model'>
	self inform: 'All informations are cleared (except the path). Set or read layout again.'.
	RWDiagram refresh.