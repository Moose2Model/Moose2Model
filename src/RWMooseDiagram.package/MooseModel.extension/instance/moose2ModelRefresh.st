*RWMooseDiagram
moose2ModelRefresh
	<menuItem: 'Refresh (Do after changing the model, clears all settings)' category: 'Moose2Model'>
	self inform: 'All informations are cleared (except the path). Set or read layout again.'.
	RWDiagram refresh.